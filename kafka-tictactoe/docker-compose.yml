version: '3.8'

# NOTE: Kafka is no longer needed!
# The application now uses WebSockets (Socket.IO) for real-time communication.
# The docker-compose file is provided for reference but is not required.

# If you still want to use Kafka for other purposes, uncomment below:
# services:
#   broker:
#     image: apache/kafka:latest
#     container_name: broker
#     ports:
#       - "9092:9092"
#     ...

